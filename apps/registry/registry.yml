version: 0.1
log:
    fields:
        service: registry
storage:
    cache:
        blobdescriptor: inmemory
    filesystem:
        rootdirectory: /var/lib/registry
http:
    addr: :5000
    headers:
        X-Content-Type-Options: [nosniff]
    tls:
        certificate: /etc/docker/registry/registry.crt
        key: /etc/docker/registry/registry.key
health:
    storagedriver:
        enabled: true
        interval: 10s
        threshold: 3
auth:
    token:
        realm: "https://registry_auth:5001/auth"
        service: "Docker registry"
        issuer: "Acme auth server"
        rootcertbundle: /etc/docker/registry/auth.crt
