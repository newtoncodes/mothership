version: "3.3"

volumes:
    mothership_xwiki_data:
        external: true
    mothership_xwiki_mysql:
        external: true

networks:
    mothership:
        external: true

services:
    xwiki_mysql:
        image: newtoncodes/mysql:5.7
        networks:
            - mothership
        volumes:
            - mothership_xwiki_mysql:/var/lib/mysql
        deploy:
            replicas: 1
            placement:
                constraints:
                    - node.role == manager

    xwiki:
        image: xwiki:9.11.1
        networks:
            - mothership
        environment:
            - DB_DATABASE=xwiki
            - DB_USER=xwiki
            - DB_PASSWORD={{PASSWORD}}
            - DB_HOST=xwiki_mysql
        volumes:
            - mothership_xwiki_data:/usr/local/xwiki
        deploy:
            replicas: 1
            placement:
                constraints:
                    - node.role == manager
